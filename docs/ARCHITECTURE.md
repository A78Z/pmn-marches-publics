# 🏛️ Architecture Technique - PMN Marchés Publics

## Vue d'ensemble

```
┌─────────────────────────────────────────────────────────────────────┐
│                         UTILISATEURS                                 │
│                   (Artisans / Administrateurs)                       │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      FRONTEND (Next.js 15)                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌────────────┐ │
│  │   Pages     │  │ Composants  │  │   Hooks     │  │  Services  │ │
│  │   (SSR)     │  │  (Shadcn)   │  │  (React)    │  │  (Parse)   │ │
│  └─────────────┘  └─────────────┘  └─────────────┘  └────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    ▼                               ▼
┌────────────────────────────┐    ┌────────────────────────────────────┐
│     BACKEND (NestJS)       │    │         BACK4APP (Parse)           │
│  ┌──────────────────────┐  │    │  ┌────────────────────────────┐   │
│  │   Scraping Module    │  │    │  │     Database (MongoDB)     │   │
│  │   (Playwright)       │──┼───▶│  │   - AppelOffre             │   │
│  └──────────────────────┘  │    │  │   - Artisan                │   │
│  ┌──────────────────────┐  │    │  │   - Favori                 │   │
│  │  Classification      │  │    │  │   - Historique             │   │
│  │  Engine              │  │    │  └────────────────────────────┘   │
│  └──────────────────────┘  │    │  ┌────────────────────────────┐   │
│  ┌──────────────────────┐  │    │  │      Cloud Functions       │   │
│  │   Notifications      │  │    │  │   - Alertes                │   │
│  │   (Email/WhatsApp)   │  │    │  │   - Triggers               │   │
│  └──────────────────────┘  │    │  └────────────────────────────┘   │
│  ┌──────────────────────┐  │    │  ┌────────────────────────────┐   │
│  │   CRON Jobs          │  │    │  │        File Storage        │   │
│  │   (Scheduled)        │  │    │  │   - Documents PDF          │   │
│  └──────────────────────┘  │    │  └────────────────────────────┘   │
└────────────────────────────┘    └────────────────────────────────────┘
                │
                ▼
┌────────────────────────────────────────────────────────────────────┐
│                    SOURCE EXTERNE                                   │
│              http://www.marchespublics.sn                          │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  Pages HTML → Parsing → Extraction → Classification          │  │
│  └──────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────┘
```

## Flux de données

### 1. Flux de Scraping

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   CRON      │───▶│  Playwright │───▶│   Parser    │───▶│ Classifier  │
│   Trigger   │    │   Browser   │    │   HTML      │    │   Engine    │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
                                                                │
                                                                ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Notif.    │◀───│  Diff       │◀───│  Validate   │◀───│  Transform  │
│   Service   │    │  Detector   │    │   Data      │    │   Data      │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
                          │
                          ▼
                   ┌─────────────┐
                   │  Back4App   │
                   │   Save      │
                   └─────────────┘
```

### 2. Flux Utilisateur

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Artisan   │───▶│   Next.js   │───▶│   Parse     │───▶│  Back4App   │
│   Request   │    │   Server    │    │   SDK       │    │   Query     │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
                                                                │
                                                                ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Render    │◀───│  Transform  │◀───│   Cache     │◀───│   Results   │
│   UI        │    │   Response  │    │   Layer     │    │   MongoDB   │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
```

## Composants détaillés

### Frontend (Next.js 15)

```
frontend/
├── app/
│   ├── (auth)/                 # Routes authentification
│   │   ├── connexion/
│   │   └── inscription/
│   ├── (dashboard)/            # Routes tableau de bord
│   │   ├── admin/
│   │   └── artisan/
│   ├── appels-offres/          # Routes publiques
│   │   ├── [id]/
│   │   └── page.tsx
│   ├── api/                    # API Routes
│   │   ├── auth/
│   │   ├── parse/
│   │   └── webhook/
│   ├── layout.tsx
│   └── page.tsx
├── components/
│   ├── ui/                     # Shadcn components
│   ├── forms/                  # Formulaires
│   ├── cards/                  # Cartes
│   └── layout/                 # Layout components
├── lib/
│   ├── parse/                  # Parse SDK client
│   ├── utils/                  # Utilitaires
│   └── hooks/                  # Custom hooks
└── styles/
    └── globals.css
```

### Backend (NestJS)

```
backend/
├── src/
│   ├── main.ts
│   ├── app.module.ts
│   ├── config/
│   │   ├── configuration.ts
│   │   └── validation.ts
│   ├── scraping/
│   │   ├── scraping.module.ts
│   │   ├── scraping.service.ts
│   │   ├── scraping.controller.ts
│   │   ├── parsers/
│   │   │   ├── marchespublics.parser.ts
│   │   │   └── base.parser.ts
│   │   └── dto/
│   │       └── appel-offre.dto.ts
│   ├── classification/
│   │   ├── classification.module.ts
│   │   ├── classification.service.ts
│   │   ├── rules/
│   │   │   ├── entretiens.rules.ts
│   │   │   ├── tenues.rules.ts
│   │   │   └── achats.rules.ts
│   │   └── keywords.constant.ts
│   ├── notifications/
│   │   ├── notifications.module.ts
│   │   ├── email.service.ts
│   │   └── whatsapp.service.ts
│   ├── parse/
│   │   ├── parse.module.ts
│   │   ├── parse.service.ts
│   │   └── repositories/
│   │       ├── appel-offre.repository.ts
│   │       └── artisan.repository.ts
│   └── tasks/
│       ├── tasks.module.ts
│       └── scraping.task.ts
└── test/
```

## Sécurité

### Niveaux d'accès

```
┌─────────────────────────────────────────────────────────────┐
│                    NIVEAU ADMINISTRATEUR                     │
│  - Accès complet aux données                                │
│  - Gestion des utilisateurs                                 │
│  - Configuration du système                                 │
│  - Déclenchement manuel du scraping                         │
│  - Visualisation des logs                                   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    NIVEAU ARTISAN                            │
│  - Consultation des appels d'offres                         │
│  - Gestion des favoris personnels                           │
│  - Configuration des alertes                                │
│  - Historique personnel                                     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    NIVEAU PUBLIC                             │
│  - Consultation limitée des appels d'offres                 │
│  - Recherche basique                                        │
│  - Inscription                                              │
└─────────────────────────────────────────────────────────────┘
```

### ACL Parse Server

```javascript
// AppelOffre - Public Read, Admin Write
{
  "ACL": {
    "*": { "read": true },
    "role:admin": { "read": true, "write": true }
  }
}

// Favori - Owner Read/Write
{
  "ACL": {
    "<userId>": { "read": true, "write": true }
  }
}

// Artisan - Owner Read, Admin Read/Write
{
  "ACL": {
    "<userId>": { "read": true },
    "role:admin": { "read": true, "write": true }
  }
}
```

## Résilience

### Gestion des erreurs de scraping

```
┌─────────────────┐
│  Start Scrape   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐     ┌─────────────────┐
│  Load Page      │────▶│  Timeout?       │
└────────┬────────┘     └────────┬────────┘
         │                       │ Yes
         │ No                    ▼
         ▼              ┌─────────────────┐
┌─────────────────┐     │  Retry (3x)     │
│  Parse HTML     │     └────────┬────────┘
└────────┬────────┘              │
         │                       ▼
         │              ┌─────────────────┐
         ▼              │  Alert Admin    │
┌─────────────────┐     └─────────────────┘
│  Structure OK?  │
└────────┬────────┘
         │ No
         ▼
┌─────────────────┐
│  Log Change     │
│  Use Fallback   │
└─────────────────┘
```

### Stratégie de fallback

1. **Structure HTML modifiée** → Utiliser les sélecteurs alternatifs
2. **Site inaccessible** → Réessayer avec backoff exponentiel
3. **Données incomplètes** → Marquer comme "à vérifier" dans la base
4. **Échec total** → Notification admin + conservation données précédentes

## Performance

### Caching Strategy

```
┌─────────────────────────────────────────────────────────────┐
│                      CACHE LAYERS                            │
├─────────────────────────────────────────────────────────────┤
│  L1: React Query Cache (Frontend)        │ TTL: 5 min      │
│  L2: Next.js ISR Cache                   │ TTL: 1 hour     │
│  L3: Parse Query Cache                   │ TTL: 15 min     │
│  L4: CDN Edge Cache (Vercel)             │ TTL: 1 hour     │
└─────────────────────────────────────────────────────────────┘
```

### Optimisations

- **Images** : Optimisation automatique via Next.js Image
- **Bundle** : Code splitting par route
- **Queries** : Pagination et lazy loading
- **Scraping** : Pool de workers pour parallélisation

## Monitoring

### Métriques clés

| Métrique | Seuil | Action |
|----------|-------|--------|
| Scraping Success Rate | < 95% | Alert |
| Response Time | > 2s | Investigate |
| Error Rate | > 1% | Alert |
| New Tenders/Day | 0 | Check source |

### Logs

```
{
  "timestamp": "2026-01-28T10:00:00Z",
  "level": "info",
  "service": "scraping",
  "event": "scrape_complete",
  "data": {
    "duration_ms": 15000,
    "new_tenders": 12,
    "updated_tenders": 5,
    "errors": 0
  }
}
```
